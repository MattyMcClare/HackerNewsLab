import React from 'react';

const StorySelector = (props) => {



    return (
        <select>
            <option
                defaultValue="default">
                select a story...
            </option>
        </select>
    )
}

export default StorySelector;